"""Shared pytest fixtures for the Multi-Agent Ops Assistant test suite."""

import os
import sys

# Ensure src/ is on the path for imports
sys.path.insert(0, os.path.join(os.path.dirname(__file__), "src"))
